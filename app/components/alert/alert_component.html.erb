<div
  class="alert alert-<%= variant %> <%= dismissible_classes %> <%= icon_classes %>"
  role="alert"
>
  <%= message %>
  <% if dismissible? %>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close">
    </button>
  <% end %>
</div>